export interface TaskTableStyles {
  root: string;
  empty: string;
}

const styles: TaskTableStyles = {
  root: "overflow-hidden rounded-xl border border-border-subtle bg-surface-overlay/40",
  empty: "px-4 py-10 text-center text-sm text-ebony-400",
};

export { styles };
