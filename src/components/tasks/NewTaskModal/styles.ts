export interface NewTaskModalStyles {
  form: string;
  grid: string;
  actions: string;
}

const styles: NewTaskModalStyles = {
  form: "flex flex-col gap-4",
  grid: "grid grid-cols-2 gap-3",
  actions: "flex justify-end gap-2 pt-1",
};

export { styles };
