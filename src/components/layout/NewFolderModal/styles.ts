export interface NewFolderModalStyles {
  form: string;
  actions: string;
}

const styles: NewFolderModalStyles = {
  form: "flex flex-col gap-4",
  actions: "flex justify-end gap-2 pt-1",
};

export { styles };
