import type { EntityPagePlaceholderStyles } from "./types";

const styles: EntityPagePlaceholderStyles = {
  root: "flex h-full flex-col gap-4 p-6",
  header: "flex flex-col gap-1",
  title: "truncate text-xl font-bold text-white",
  description: "text-sm text-ebony-400",
};

export { styles };
