import type { PriorityBadgeStyles } from "./types";

const styles: PriorityBadgeStyles = {
  root: "inline-flex items-center gap-1 text-xs font-medium",
  icon: "size-3.5 fill-current opacity-70",
};

export { styles };
